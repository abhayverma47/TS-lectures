import { version } from "typescript";

console.log(
  "Hello, Welcome to my Typescript Youtube series, it seems you have setup the repo properly, let's get started!"
);
console.log("You are running TypeScript version:", version);
